# Phase 1 scaffolding placeholder.
# Marks the `config` directory as a Python package. This package will hold
# the Django project-level configuration (settings, URL routing, WSGI entry
# point) that ties all apps together.
