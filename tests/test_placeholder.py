"""Placeholder test for Phase 1/2 CI plumbing.

This will be replaced by real Django tests once models/views exist.
"""


def test_placeholder():
    # Exists only to give pytest something to run, so the structured-output
    # pipeline (pytest -> JUnit XML -> uploaded artifact) can be verified
    # end to end before there's real test coverage.
    assert True
